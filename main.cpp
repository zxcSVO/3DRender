#include"libs/cppLibs.h"

int main() {
    // polygon pol{vec3(0, 10, 0), vec3(0, 10, 1), vec3(1, 10, 0)};
    // std::cout << pol << std::endl;
    // pol.rotateXY(M_PI / 2);
    // std::cout << pol;
    return 0;
}
// добавить преобразование точек в базис камеры
